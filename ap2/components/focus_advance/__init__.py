"""focus_advance component subpackage marker (TB-310 stub).

The flat module `ap2/focus_advance.py` still owns the implementation —
this subpackage exists only to register the component via
`manifest.py` so `Registry.discover()` picks it up. The structural
relocation belongs to axis (5) of the components focus
(goal.md L116-201).
"""
